import { css } from '@emotion/native';

import { COLOR } from '_constants';

export const styles = {
  backGroundColor: css`
    background-color: ${COLOR.LIGHT_GRAY};
  `,
  body: css`
    padding: 30px 0;
  `,
  container: css`
    padding: 0 15px;
  `
};
